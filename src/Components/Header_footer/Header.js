import React, { Component } from 'react';
import './Header.css';
import classes from './Header.css';

class Header extends Component{
    render(){
        return(
            <button type="button" className="btn btn-primary">Worked Out!!</button>
        )
    }
}

export default Header;